$('#box-navi .item').tab();
$('.ui.checkbox').checkbox();
$('.dropdown').dropdown();
$('.no.label.ui.dropdown').dropdown({
    useLabels: false
});
$('.sticky').sticky({
    context: '.docContent',
    pushing: true
});
$('.docMenu .item').tab();